Vendor Master 


VENDOR V2---

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORACCOUNTNUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(VENDORACCOUNTNUMBER) > 20

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSCITY exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(ADDRESSCITY) > 60

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSCOUNTRYREGIONID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(ADDRESSCOUNTRYREGIONID) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSDESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(ADDRESSDESCRIPTION) > 60

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSSTATEID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(ADDRESSSTATEID) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSSTREET exceeds max length 250','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(ADDRESSSTREET) > 250

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSSTREETNUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(ADDRESSSTREETNUMBER) > 20

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSZIPCODE exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(ADDRESSZIPCODE) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'BANKACCOUNTID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(BANKACCOUNTID) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CURRENCYCODE exceeds max length 3','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(CURRENCYCODE) > 3

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'DEFAULTDELIVERYMODEID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(DEFAULTDELIVERYMODEID) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'DEFAULTDELIVERYTERMSCODE exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(DEFAULTDELIVERYTERMSCODE) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'DEFAULTLEDGERDIMENSIONDISPLAYVALUE exceeds max length 500','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(DEFAULTLEDGERDIMENSIONDISPLAYVALUE) > 500

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'DEFAULTOFFSETLEDGERACCOUNTDISPLAYVALUE exceeds max length 500','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(DEFAULTOFFSETLEDGERACCOUNTDISPLAYVALUE) > 500

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'DEFAULTPAYMENTTERMSNAME exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(DEFAULTPAYMENTTERMSNAME) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'DEFAULTVENDORPAYMENTMETHODNAME exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(DEFAULTVENDORPAYMENTMETHODNAME) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'INVOICEVENDORACCOUNTNUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(INVOICEVENDORACCOUNTNUMBER) > 20

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'LANGUAGEID exceeds max length 7','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(LANGUAGEID) > 7

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PAYMENTSPECIFICATIONID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PAYMENTSPECIFICATIONID) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYEMAILADDRESS exceeds max length 255','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PRIMARYEMAILADDRESS) > 255

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYEMAILADDRESSDESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PRIMARYEMAILADDRESSDESCRIPTION) > 60

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYEMAILADDRESSPURPOSE exceeds max length 100','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PRIMARYEMAILADDRESSPURPOSE) > 100

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYPHONENUMBER exceeds max length 255','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PRIMARYPHONENUMBER) > 255

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYPHONENUMBERDESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PRIMARYPHONENUMBERDESCRIPTION) > 60

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYPHONENUMBEREXTENSION exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PRIMARYPHONENUMBEREXTENSION) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYPHONENUMBERPURPOSE exceeds max length 100','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PRIMARYPHONENUMBERPURPOSE) > 100

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SALESTAXGROUPCODE exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(SALESTAXGROUPCODE) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORGROUPID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(VENDORGROUPID) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORKNOWNASNAME exceeds max length 100','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(VENDORKNOWNASNAME) > 100

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORORGANIZATIONNAME exceeds max length 100','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(VENDORORGANIZATIONNAME) > 100

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORPARTYTYPE exceeds max length 13','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(VENDORPARTYTYPE) > 13

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORSEARCHNAME exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(VENDORSEARCHNAME) > 20


UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYCONTACTPERSONID exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(PRIMARYCONTACTPERSONID) > 20

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'COMPANYCHAINNAME exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(COMPANYCHAINNAME) > 20

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'MAINCONTACTPERSONNELNUMBER exceeds max length 25','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(MAINCONTACTPERSONNELNUMBER) > 25

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'MFGProSupplier exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(MFGProSupplier) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'MFGPRoDomain exceeds max length 4','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(MFGPRoDomain) > 4


UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CategoryCode exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(CategoryCode) > 20

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SubCategoryCode exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(SubCategoryCode) > 20


UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CategoryToContinue exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(CategoryToContinue) > 60

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SupplierRiskCategory exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(SupplierRiskCategory) > 60

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'LegalEntityID exceeds max length 4','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(LegalEntityID) > 4

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'WITHHOLDINGTAXGROUPCODE exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(WITHHOLDINGTAXGROUPCODE) > 10

UPDATE PWC_E_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'TAXEXEMPTNUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDOR	
	WHERE 
		LEN(TAXEXEMPTNUMBER) > 20















---------- Payment Days 

UPDATE PWC_E_VENDPAYMENTDAY
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'Name exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDPAYMENTDAY	
	WHERE 
		LEN(Name) > 10

UPDATE PWC_E_VENDPAYMENTDAY
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'Description exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDPAYMENTDAY	
	WHERE 
		LEN(Description) > 60




















Vendor Addresses--

UPDATE PWC_E_VENDADDRESS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORLEGALENTITYID exceeds max length 4','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDADDRESS	
	WHERE 
		LEN(VENDORLEGALENTITYID) > 4

UPDATE PWC_E_VENDADDRESS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORACCOUNTNUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDADDRESS	
	WHERE 
		LEN(VENDORACCOUNTNUMBER) > 20


UPDATE PWC_E_VENDADDRESS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSCITY exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDADDRESS	
	WHERE 
		LEN(ADDRESSCITY) > 60


UPDATE PWC_E_VENDADDRESS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSCOUNTRYREGIONID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDADDRESS	
	WHERE 
		LEN(ADDRESSCOUNTRYREGIONID) > 10


UPDATE PWC_E_VENDADDRESS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSDESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDADDRESS	
	WHERE 
		LEN(ADDRESSDESCRIPTION) > 60


UPDATE PWC_E_VENDADDRESS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSSTATEID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDADDRESS	
	WHERE 
		LEN(ADDRESSSTATEID) > 10


UPDATE PWC_E_VENDADDRESS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSSTREET exceeds max length 250','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDADDRESS	
	WHERE 
		LEN(ADDRESSSTREET) > 250


UPDATE PWC_E_VENDADDRESS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSSTREETNUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDADDRESS	
	WHERE 
		LEN(ADDRESSSTREETNUMBER) > 20









Vendor Payment Method---

UPDATE PWC_E_VENDORPAYMENTMETHOD
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'Description exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORPAYMENTMETHOD	
	WHERE 
		LEN(Description) > 60

UPDATE PWC_E_VENDORPAYMENTMETHOD
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'Name exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORPAYMENTMETHOD	
	WHERE 
		LEN(Name) > 10



Vendor Certification Type---

UPDATE PWC_E_VENDVENDORCERTIFICATIONTPE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'Description exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDVENDORCERTIFICATIONTPE	
	WHERE 
		LEN(Description) > 60

UPDATE PWC_E_VENDVENDORCERTIFICATIONTPE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'Name exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDVENDORCERTIFICATIONTPE	
	WHERE 
		LEN(Name) > 10


Tax Registration ID Vendor---

UPDATE PWC_E_TAXSERVICETAXREGISTRATIONNUMBERVENDORS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'RegistrationNumber exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXSERVICETAXREGISTRATIONNUMBERVENDORS
	WHERE 
		LEN(RegistrationNumber) > 60

UPDATE PWC_E_TAXSERVICETAXREGISTRATIONNUMBERVENDORS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'TaxRegstrationType exceeds max length 30','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXSERVICETAXREGISTRATIONNUMBERVENDORS
	WHERE 
		LEN(TaxRegstrationType) > 30



Line of Business---

UPDATE PWC_E_LINEOFBUSINESSSTAGING_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'Description exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_LINEOFBUSINESSSTAGING_VENDOR
	WHERE 
		LEN(Description) > 60

UPDATE PWC_E_LINEOFBUSINESSSTAGING_VENDOR
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'LINEOFBUSINESS exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_LINEOFBUSINESSSTAGING_VENDOR
	WHERE 
		LEN(LINEOFBUSINESS) > 10

Vendor Groups---

UPDATE PWC_E_VENDVENDORGROUPSTAGING
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORGROUPID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDVENDORGROUPSTAGING
	WHERE 
		LEN(VENDORGROUPID) > 10

UPDATE PWC_E_VENDVENDORGROUPSTAGING
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'Description exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDVENDORGROUPSTAGING
	WHERE 
		LEN(Description) > 60

UPDATE PWC_E_VENDVENDORGROUPSTAGING
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SummaryAccount exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDVENDORGROUPSTAGING
	WHERE 
		LEN(SummaryAccount) > 60


UPDATE PWC_E_VENDVENDORGROUPSTAGING
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'OffsetAccount exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDVENDORGROUPSTAGING
	WHERE 
		LEN(OffsetAccount) > 60


UPDATE PWC_E_VENDVENDORGROUPSTAGING
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'LiquidityAccountForPayment exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDVENDORGROUPSTAGING
	WHERE 
		LEN(LiquidityAccountForPayment) > 60

Vendor Business Segments---

UPDATE PWC_E_VENDBUSINESSSEGMENT
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SegmentDescription exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDBUSINESSSEGMENT
	WHERE 
		LEN(SegmentDescription) > 60

UPDATE PWC_E_VENDBUSINESSSEGMENT
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SegmentCode exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDBUSINESSSEGMENT
	WHERE 
		LEN(SegmentCode) > 20

Vendor Business SubSegments---

UPDATE PWC_E_VENDBUSINESSSUBSEGMENT
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'BusinessSegmentCode exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDBUSINESSSUBSEGMENT
	WHERE 
		LEN(BusinessSegmentCode) > 20

UPDATE PWC_E_VENDBUSINESSSUBSEGMENT
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SubsegmentDescription exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDBUSINESSSUBSEGMENT
	WHERE 
		LEN(BusinessSegmentCode) > 60

UPDATE PWC_E_VENDBUSINESSSUBSEGMENT
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SubsegmentCode exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDBUSINESSSUBSEGMENT
	WHERE 
		LEN(BusinessSegmentCode) > 20

Company Chains---

UPDATE PWC_E_VENDCOMPANYCHAIN
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CHAINNAME exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDCOMPANYCHAIN
	WHERE 
		LEN(CHAINNAME) > 20

UPDATE PWC_E_VENDCOMPANYCHAIN
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CHAINDESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDCOMPANYCHAIN
	WHERE 
		LEN(CHAINDESCRIPTION) > 60

Vendor Payment Method Specification---

UPDATE PWC_E_VENDORPAYMENTMETHODSPEC
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CLASSNAME exceeds max length 81','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORPAYMENTMETHODSPEC
	WHERE 
		LEN(CLASSNAME) > 81

UPDATE PWC_E_VENDORPAYMENTMETHODSPEC
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'DESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORPAYMENTMETHODSPEC
	WHERE 
		LEN(DESCRIPTION) > 60

UPDATE PWC_E_VENDORPAYMENTMETHODSPEC
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PAYMENTMETHODNAME exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORPAYMENTMETHODSPEC
	WHERE 
		LEN(PAYMENTMETHODNAME) > 10

UPDATE PWC_E_VENDORPAYMENTMETHODSPEC
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'NAME exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORPAYMENTMETHODSPEC
	WHERE 
		LEN(NAME) > 10

Vendor Contacts---

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CONTACTPERSONPARTYTYPE exceeds max length 13','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(CONTACTPERSONPARTYTYPE) > 13

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ASSOCIATEDPARTYNUMBER exceeds max length 40','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(ASSOCIATEDPARTYNUMBER) > 40

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYEMAILADDRESSPURPOSE exceeds max length 1000','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(PRIMARYEMAILADDRESSPURPOSE) > 1000

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CONTACTPERSONID exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(CONTACTPERSONID) > 20


UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CONTACTPERSONNAME exceeds max length 160','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(CONTACTPERSONNAME) > 160


UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'FIRSTNAME exceeds max length 50','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(FIRSTNAME) > 50


UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'LASTNAME exceeds max length 50','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(LASTNAME) > 50


UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYADDRESSCOUNTRYREGIONID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(PRIMARYADDRESSCOUNTRYREGIONID) > 10


UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYEMAILADDRESS exceeds max length 255','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(PRIMARYEMAILADDRESS) > 255

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYEMAILADDRESSDESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(PRIMARYEMAILADDRESSDESCRIPTION) > 60

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYPHONENUMBER exceeds max length 255','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(PRIMARYPHONENUMBER) > 255

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYPHONENUMBEREXTENSION exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(PRIMARYPHONENUMBEREXTENSION) > 10

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYTELEX exceeds max length 255','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(PRIMARYTELEX) > 255

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PRIMARYSALUTATIONPHRASE exceeds max length 25','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(PRIMARYSALUTATIONPHRASE) > 25

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ALTERNATESALUTATIONPHRASE exceeds max length 25','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(ALTERNATESALUTATIONPHRASE) > 25


UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ELECTRONICLOCATIONID exceeds max length 40','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(ELECTRONICLOCATIONID) > 40

UPDATE PWC_E_VENDORCONTACTSV2
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'APCONTACT exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORCONTACTSV2
	WHERE 
		LEN(APCONTACT) > 60


Registration Numbers---
UPDATE PWC_E_TAXREGISTRATIONLEGISLATIVE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'TAXREGSTRATIONTYPE exceeds max length 30','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXREGISTRATIONLEGISLATIVE
	WHERE 
		LEN(TAXREGSTRATIONTYPE) > 30

UPDATE PWC_E_TAXREGISTRATIONLEGISLATIVE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'COUNTRYREGIONID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXREGISTRATIONLEGISLATIVE
	WHERE 
		LEN(COUNTRYREGIONID) > 10

UPDATE PWC_E_TAXREGISTRATIONLEGISLATIVE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'LOCATIONID exceeds max length 40','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXREGISTRATIONLEGISLATIVE
	WHERE 
		LEN(LOCATIONID) > 40

UPDATE PWC_E_TAXREGISTRATIONLEGISLATIVE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'PARTYNUMBER exceeds max length 40','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXREGISTRATIONLEGISLATIVE
	WHERE 
		LEN(PARTYNUMBER) > 40

UPDATE PWC_E_TAXREGISTRATIONLEGISLATIVE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'REGISTRATIONNUMBER exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXREGISTRATIONLEGISLATIVE
	WHERE 
		LEN(REGISTRATIONNUMBER) > 60

UPDATE PWC_E_TAXREGISTRATIONLEGISLATIVE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'DESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXREGISTRATIONLEGISLATIVE
	WHERE 
		LEN(DESCRIPTION) > 60

UPDATE PWC_E_TAXREGISTRATIONLEGISLATIVE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ISSUER exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXREGISTRATIONLEGISLATIVE
	WHERE 
		LEN(ISSUER) > 60

UPDATE PWC_E_TAXREGISTRATIONLEGISLATIVE
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SECTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_TAXREGISTRATIONLEGISLATIVE
	WHERE 
		LEN(SECTION) > 60







        

Vendor Bank Account---

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORACCOUNTNUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(VENDORACCOUNTNUMBER) > 20

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'VENDORBANKACCOUNTID exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(VENDORBANKACCOUNTID) > 10

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSCITY exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(ADDRESSCITY) > 60

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSCOUNTRY exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(ADDRESSCOUNTRY) > 10


UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSCOUNTRYISOCODE exceeds max length 2','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(ADDRESSCOUNTRYISOCODE) > 2

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSDESCRIPTION exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(ADDRESSDESCRIPTION) > 60

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSSTATE exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(ADDRESSSTATE) > 10

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSSTREET exceeds max length 250','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(ADDRESSSTREET) > 250

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ADDRESSZIPCODE exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(ADDRESSZIPCODE) > 10

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'BANKACCOUNTNUMBER exceeds max length 48','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(BANKACCOUNTNUMBER) > 48

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'BANKNAME exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(BANKNAME) > 60

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CURRENTCURRENCYCODE exceeds max length 3','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(CURRENTCURRENCYCODE) > 3

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'ROUTINGNUMBER exceeds max length 12','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(ROUTINGNUMBER) > 12

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'SWIFTCODE exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(SWIFTCODE) > 20

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'IBAN exceeds max length 48','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(IBAN) > 48

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CONTACTNAME exceeds max length 60','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(VENDORACCOUNTNUMBER) > 60

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CONTACTPHONENUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(CONTACTPHONENUMBER) > 20

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CONTACTMOBILEPHONENUMBER exceeds max length 20','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(CONTACTMOBILEPHONENUMBER) > 20

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CONTACTPHONENUMBEREXTENSION exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(CONTACTPHONENUMBEREXTENSION) > 10

UPDATE PWC_E_VENDORBANKACCOUNTS
	SET ERRORDESC2 = CONCAT(ERRORDESC2,'CONTACTTELEXNUMBER exceeds max length 10','; ')
	,ERRORSTATUS2 = CONCAT(ERRORSTATUS2, 'Constraint', '; ')
	FROM PWC_E_VENDORBANKACCOUNTS
	WHERE 
		LEN(CONTACTTELEXNUMBER) > 10

